# user-service.yml
server:
  port: 8081
  forward-headers-strategy: framework

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/user_db
    username: root
    password: 2005@Kalyansai
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: kalyan12218949@gmail.com # Update with your email
    password: yaqa wkri xigj ycbl    # Update with your app password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: my-super-secret-key-12345678901234567890

management:
  tracing:
    sampling:
      probability: 1.0

  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    hostname: user-service
    prefer-ip-address: true
